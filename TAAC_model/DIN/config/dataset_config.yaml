taac2026_tiny:
    data_root: /data/lc/FuxiCTR-main/data/TAAC/datasets/taac_flat
    data_format: parquet
    train_data: /data/lc/FuxiCTR-main/data/TAAC/datasets/taac_flat/train.parquet
    valid_data: /data/lc/FuxiCTR-main/data/TAAC/datasets/taac_flat/valid.parquet
    test_data:  /data/lc/FuxiCTR-main/data/TAAC/datasets/taac_flat/test.parquet
    min_categr_count: 1
    label_col:
        name: label
        dtype: float
    feature_cols:
        # ─── ID 特征 ───────────────────────────────────────────
        - {name: user_id, active: True, dtype: str, type: categorical, embedding_dim: 16}
        - {name: item_id, active: True, dtype: str, type: categorical, embedding_dim: 16}

        # ─── 用户 int_value（categorical）共 41 个 ─────────────
        - {name: uf_1,   active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_3,   active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_4,   active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_50,  active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_51,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_52,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_55,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_56,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_57,  active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_58,  active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: uf_59,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_60,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_61,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_62,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_63,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_64,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_65,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_66,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_76,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_80,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_82,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_86,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_87,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_88,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_89,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_90,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_91,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_92,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_93,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_94,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_95,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_96,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_97,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_98,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_99,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_100, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_101, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_102, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_103, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_104, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: uf_105, active: True, dtype: str, type: categorical, embedding_dim: 4}

        # ─── 用户 int_array（sequence）共 6 个 ──────────────────
        - {name: uf_5,  active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_18, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_53, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_54, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_67, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_74, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}

        # ─── 用户 int_array+float_array（sequence, 取ID部分）共 8 个 ─
        - {name: uf_69, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_70, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_71, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_72, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_73, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_83, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_84, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}
        - {name: uf_85, active: True, dtype: str, type: sequence, splitter: "^", max_len: 20, padding: post, embedding_dim: 8}


        # # ─── 用户 int_array+float_array（sequence, 取ID部分）共 8 个 ─
        - {name: uf_69_float, active: True, dtype: float, type: embedding, pretrain_dim: 4, embedding_dim: 4}
        - {name: uf_85_float, active: True, dtype: float, type: embedding, pretrain_dim: 10, embedding_dim: 10}
        - {name: uf_83_float, active: True, dtype: float, type: embedding, pretrain_dim: 10, embedding_dim: 10}
        - {name: uf_70_float, active: True, dtype: float, type: embedding, pretrain_dim: 4, embedding_dim: 4}
        - {name: uf_72_float, active: True, dtype: float, type: embedding, pretrain_dim: 5, embedding_dim: 5}
        - {name: uf_84_float, active: True, dtype: float, type: embedding, pretrain_dim: 10, embedding_dim: 10}
        - {name: uf_71_float, active: True, dtype: float, type: embedding, pretrain_dim: 6, embedding_dim: 6}
        - {name: uf_73_float, active: True, dtype: float, type: embedding, pretrain_dim: 5, embedding_dim: 5}


        # # ─── 物品 int_value（categorical）共 14 个 ──────────────
        - {name: if_6,  active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: if_7,  active: True, dtype: str, type: categorical, embedding_dim: 8}
        - {name: if_8,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_9,  active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_10, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_11, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_12, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_13, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_15, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_16, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_75, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_77, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_78, active: True, dtype: str, type: categorical, embedding_dim: 4}
        - {name: if_79, active: True, dtype: str, type: categorical, embedding_dim: 4}

        # # ─── 物品 int_array（sequence）共 1 个 ──────────────────
        - {name: if_14, active: True, dtype: str, type: sequence, splitter: "^", max_len: 10, padding: post, embedding_dim: 8}

        # # ─── 行为序列 action_seq（sequence）共 10 个 ────────────
        - {name: aseq_19, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_20, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: aseq_21, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_22, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_23, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_24, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_25, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_26, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_27, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}
        - {name: aseq_28, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 4, feature_encoder: null}

        # # # ─── 内容序列 content_seq（sequence）共 9 个 ────────────
        - {name: cseq_40, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_41, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_42, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_43, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_44, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_45, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_46, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_47, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: cseq_48, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}

        # # ─── 历史物品序列 item_seq（sequence）共 12 个 ──────────
        - {name: iseq_29, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_30, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null, share_embedding: "if_6"}
        - {name: iseq_31, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null, share_embedding: "if_7"}
        - {name: iseq_32, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_33, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_34, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_35, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_36, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 16, feature_encoder: null, share_embedding: "item_id"}
        - {name: iseq_37, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_38, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_39, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
        - {name: iseq_49, active: True, dtype: str, type: sequence, splitter: "^", max_len: 50, padding: pre, embedding_dim: 8, feature_encoder: null}
